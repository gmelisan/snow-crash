Путем недолгих разбирательств выясняется, что при выполнении level09
переданная в качестве аргумента строка кодируется по принципу

abcd -> aceg

Т.е. символ новой строки = символ старой + его позиция, начиная с 0.

В токене лежит строка. Тут ничего кроме того, чтобы зареверсить
алгоритм придумать нельзя. Написал простую сишную прогу для этого.
Надо скачать с машины токен (там есть невидимые символы)
